app:
  encryption:
    key: ${ENCRYPTION_KEY:development-encryption-key-1234567890}
spring:
  application:
    name: eroaming
  datasource:
    url: jdbc:postgresql://localhost:6543/mydatabase
    username: myuser
    password: mypassword
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
